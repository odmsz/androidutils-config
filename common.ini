[default]
*enter_fastboot       = adb reboot bootloader
*enter_recovery       = adb reboot recovery
*adb_root             = adb root
*disable_verity       = adb disable_verity
*adb_remount          = adb remount
*fastboot_switch_slot = fastboot --set-active=b

